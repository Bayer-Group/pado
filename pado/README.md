# pado

```
# work in progress

from pado import PadoDataset

ds = PadoDataset("~/my_path/padodataset/pado.dataset.toml")

result = ds.query(year=2019, site=berlin, organ=liver)
# need to figure out what the itneraction will look like here...

```