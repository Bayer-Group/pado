# pado

```
# work in progress

from pado import PadoDataset

ds = PadoDataset("~/my_path/pado_dataset")

result = ds.query(year=2019, site=berlin, organ=liver)
# need to figure out what the interaction will look like here...

```